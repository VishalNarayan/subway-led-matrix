#include "config.h"

// WiFi Configuration (unused — credentials are now managed via NVS provisioning)
const char* WIFI_SSID = "";
const char* WIFI_PASSWORD = "";

// API Configuration — set your Lambda function URL here
const char* API_BASE_URL = "";
